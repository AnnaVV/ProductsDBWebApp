@model MyMvcApplicationTest.Models.Location

@{
    ViewBag.Title = "AddLocation";
}

<h2>Add Location form</h2>

@*<form role="form" method="POST">
    <div class="form-group">     
        @Html.LabelFor(m => m.Name)
        @Html.TextBoxFor(x => x.Name, new { @class = "form-control", placeholder = "Enter city name here" })
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
</form>
<br/>
@Html.ActionLink("Main page","Index")*@

@using(Html.BeginForm("SaveLocation", "Home", FormMethod.Post))
{
    <div class="col-sm-5">
    @Html.TextBoxFor(x => x.Name, new { @class = "form-control", placeholder = "Имя" })
    <input type="submit" value="Save" class="btn btn-primary" style="margin-top:10px" />
    </div>
    
    
    
}
